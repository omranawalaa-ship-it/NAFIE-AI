[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nafie-ai"
version = "0.1.0"
description = "National AI Platform for Financial Fraud Response Readiness"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
